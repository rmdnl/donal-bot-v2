# DONAL Bot Pro
Multi-regime trading bot (TREND/SIDEWAYS/BEAR) + production infrastructure.
Merge terbaik dari donal-binance-bot (strategi) dan bot-binance (infra).
Setup: cp .env.example .env && pip install -r requirements.txt && python main.py --testnet
